# Navi 导航站 · 容器化开发项目总结

> 记录本次「个人导航页 + Docker 容器化」从需求到上线运行的完整过程，供后续接手者快速了解全貌。
> 状态：**已于 2026-09-20 完成 v1.1.0 发布并在 NAS 上线运行验证通过**。
> 代码仓库 `github.com/mijunyi/navi-homepage`（public）｜镜像 `ghcr.io/mijunyi/navi-homepage`（public，匿名可拉）

---

## 一、项目概述

### 1.1 功能定位

一个**自托管的个人/家庭导航页**：把家里 NAS、软路由、各种自建服务聚合到一个页面上，按分组展示卡片，支持内外网双地址、图标自动识别与自动发现局域网内的容器服务。

核心能力（均由测试套件守着）：

| 能力 | 说明 |
|---|---|
| 卡片与分组管理 | 增删改查、拖拽排序、编辑模式、批量导入 |
| 内外网双地址 | 卡片级 `netMode`（自动 / 仅内网 / 仅外网）+ 异步可达性探测 |
| 拼音搜索 / `Ctrl+K` 面板 | 标题与标签均支持全拼与首字母 |
| 卡片标签 | 跨分组检索，点标签即筛选 |
| 服务发现 | 挂 `docker.sock` 自动识别容器并匹配图标；不挂则降级扫本机端口 |
| 首页状态板 | CPU / 内存 / 磁盘 / 容器数，30s 轮询，采不到给 `null` 不给 `0` |
| 备份与恢复 | JSON 配置备份 + 含图片的 ZIP 备份（手写 ZIP，零依赖） |
| 图床库 | 227 个内置本地图标（离线可用）+ 自定义上传 |
| 密码保护 | 会话鉴权，支持明文或 SHA-256 哈希，可加用户名 |

### 1.2 容器化目标

| 目标 | 达成情况 |
|---|---|
| 零 npm 依赖、零构建步骤 | ✅ 仅 Node 内置模块，`CMD ["node","server.js"]` 直起 |
| 一份镜像跑 x86 与 ARM | ✅ CI 产出 `linux/amd64` + `linux/arm64` 双架构 manifest |
| 数据与程序彻底分离 | ✅ `data/` 整目录挂载，容器可随意重建 |
| 部署方式可选 | ✅ 源码构建 / 拉预构建镜像 / 裸 Node，三种均验证过 |
| 升级只有一句话 | ✅ `pull` + `up -d`（配合 tag 触发的 `latest`） |
| 不挂 socket 也完全可用 | ✅ 服务发现降级，其余功能不受影响（项目红线之一） |

### 1.3 最终交付成果

| 类别 | 成果 |
|---|---|
| 运行时 | `server.js` / `discovery.js` / `status.js` / `zip.js`（四个后端模块，单向依赖无环）+ 原生前端 |
| 镜像 | `ghcr.io/mijunyi/navi-homepage`：标签 `1.1.0` / `1.1` / `latest` / `edge`，双架构，public |
| 编排 | `docker-compose.yml`（源码构建）、`docker-compose.image.yml`（纯拉取，`-f` 二选一） |
| 流水线 | `.github/workflows/docker-publish.yml`：推 main 出 `edge`，打 `v*.*.*` 出 semver + `latest` |
| 质量 | **19 个测试套件 / 961 项断言 / 0 失败**（其中 9 套为真实浏览器 UI 测试） |
| 文档 | 门户站点 `docs/site.html`、项目概览 `docs/overview.html`、成果预览 `docs/showcase.html`、路线图 `docs/roadmap.html`、三份部署指南 + 三份 PDF |
| 线上 | `http://10.10.10.18:18880`（飞牛 NAS），版本 1.1.0，服务发现 18/19 容器在线 |

---

## 二、开发过程梳理

### 阶段一：需求确认与资产盘点

- 明确使用环境是**局域网 HTTP**（`http://10.10.10.18:18880`），属**非安全上下文** —— 这一条决定了后续所有涉及浏览器能力的改动都要按局域网复核（`crypto.subtle` 不可用、`fetch` 混合内容被拦）。
- 确立四条工程红线：零依赖零构建、多架构镜像必须过、**断言只增不减**、不挂 socket 时功能全可用。
- 盘点既有资产：源码、截图、部署指南，确认「程序可重建、`data/` 唯一不可再生」这一数据边界。

### 阶段二：页面与后端能力开发

按优先级分三批落地，每批都配回归断言：

| 批次 | 内容 |
|---|---|
| P0 | 拼音搜索 + `Ctrl+K` 命令面板；卡片级内外网策略与异步探测；备份含图片（手写 ZIP） |
| P1 | 首页状态板（`status.js` + `/api/status`）；图标本地化（227 个内置图标，`scripts/build-icons.cjs` 生成）；发现来源追踪与失效标记（只标记不删除） |
| P2 | 首次引导；拖链接建卡；**卡片标签（跨分组检索）**；`DOCKER_GID` 说明；`CHANGELOG.md` |

关键实现取舍：`matchRank()` 是首页网格与 `Ctrl+K` 面板的**唯一匹配入口**，所以标签检索只加在这一处，两处同时生效 —— 这是早期抽象留下的便利。

### 阶段三：Dockerfile 与镜像定义

- 基线 `node:22-alpine`，`WORKDIR /app`，显式 COPY 每个后端模块与 `public/`。
- 构建期生成 `/app/data/config.json` 占位示例，并**删除** `public/config.json`，避免镜像内出现两份会互相误导的配置。
- `VOLUME ["/app/data"]` + `ENV NAVI_CONFIG_PATH/NAVI_UPLOAD_DIR` 把写入点迁移到挂载目录。
- `EXPOSE 80`，对外端口由 compose 映射决定。

### 阶段四：CI 构建与多架构发布

- 流水线：`push main` → `edge`；`tag v*.*.*` → semver 三标签 + `latest`。
- 多架构用 QEMU + Buildx；`provenance:false, sbom:false` 必须保留，否则 manifest 多出 `unknown/unknown` 平台，老版本 Docker 拉取会报错。
- 无 Docker 环境下也能验证：GHCR 匿名令牌三连（取 token → 查 tag 列表 → 读 manifest 看 platform）。

### 阶段五：部署到 NAS 与验证

- 诊断发现原部署是**源码构建模式**（`docker-compose.yml`），长期用 `pull` 是无效的，于是切换到纯拉取模式并按实况调整 compose（端口 18880、容器名 `navi`、保留 socket 只读挂载）。
- 切换过程：备份 `data/` → 停旧容器 → 拉 1.1.0 → `up -d`。
- 验证分四层：项目自带判定脚本（`check-deploy.cjs`）、接口端到端探测（12 组）、**Playwright 真实浏览器走完登录与基本操作**、uptime 采样确认无重启循环。

### 阶段六：文档与发版

- 补三份站点：门户站点、项目概览、迭代成果预览；同时给它们的数字加护栏断言，防止文档漂移。
- 发 `v1.1.0`：统一四处硬编码版本号 → 补版本号一致性护栏 → 全量回归 → 重建发布包 → 推 main → 打 tag → 验 GHCR 产出。

---

## 三、问题与解决方案

### 3.1 镜像与构建类

| # | 现象 | 排查思路 | 解决 |
|---|---|---|---|
| 1 | 本地测试全绿，镜像内 `MODULE_NOT_FOUND`，容器起不来 | 测试直读源码目录，掩住了 COPY 缺失；对比 `server.js` 里 `require("./x")` 与 Dockerfile 的 COPY 清单 | 新增模块时**同步改 COPY 清单**；写进 `imagecompose.test.cjs`：从源码抽全部本地 require，逐个核对 COPY（双向比对） |
| 2 | 老版本 Docker 拉镜像报平台错误 | 读 manifest 发现多出 `unknown/unknown` 平台项 | 流水线固定 `provenance:false, sbom:false` |
| 3 | 镜像推送 400 / 拉不到 | 镜像名含大写 | 镜像名必须小写（`${GITHUB_REPOSITORY,,}`） |
| 4 | 语义化标签对不上 | semver 动作会剥掉 `v` 前缀 | 明确 `v1.0.0` 是 **git tag**，镜像标签是 `1.0.0`；拉 `v1.0.0` 404 属预期 |

### 3.2 端口与访问类

| # | 现象 | 排查思路 | 解决 |
|---|---|---|---|
| 5 | compose 默认 `8080:80`，与 NAS 实际端口不符 | 用 `docker port navi` 取真实映射 | 按实况生成 compose（18880），**不盲从模板** |
| 6 | HTTPS 页面探测 `http://内网IP` 恒失败，卡片全回退外网 | 非 mixed content 能成功的假设错误 | `probeAllowed()` 显式跳过并如实告知用户；**不承诺「HTTPS 下自动切换可用」** |
| 7 | `docker compose ... pull` 报 `no such file or directory` | 用户在 `~` 下执行，compose 文件在部署目录 | 用 `docker inspect` 读 compose 标签拿到工作目录与配置文件路径，再 `cd` 进去 |
| 8 | `up -d` 报容器名冲突 | 旧容器仍在跑且同名 | 先备份 `data/` → `docker stop/rm` 旧容器 → 再 `up -d`；数据因整目录挂载不受影响 |

### 3.3 静态资源类

| # | 现象 | 排查思路 | 解决 |
|---|---|---|---|
| 9 | 改了代码 / 重建镜像，浏览器行为依旧 | 前端 js/css 是 7 天强缓存，缓存键 = URL（含 `?v=`） | 服务端 `assetToken()` 按文件 mtime 自动生成版本号；排障先 `Ctrl+F5` |
| 10 | 对外发布的截图主题与标注不符（标「夜间」却是浅色图） | 另一个测试套件顺手 `page.screenshot` 覆盖了发布图 | 确立**发布截图唯一生产者**（`test/page-shots.cjs`）并写护栏：扫描所有写者、核对清单、孤儿文件必须与已知一致 |
| 11 | HTML 交付物页面看着正常但交互全不动 | 肉眼翻不出来 | 用 `vm.Script` 编译内联 JS 定位：一个跨行字符串让整个 `<script>` 编译失败。已沉淀为通用自检方法 |

### 3.4 数据持久化类

| # | 现象 | 排查思路 | 解决 |
|---|---|---|---|
| 12 | 保存配置报 `EBUSY` / `EXDEV`（飞牛 NAS 上必现） | 单文件挂载下写文件走「跨挂载点 rename」 | 改为**整目录挂载** `./data:/app/data`，rename 发生在同一挂载点内 |
| 13 | 整目录挂载后容器启动即失败 | 宿主空目录遮住了镜像内置示例配置 | 宿主机 `data/config.json` 必须先存在（从镜像 `cat` 出来或复制示例） |
| 14 | GUI Compose 部署失败 | 飞牛图形界面不支持 `build:` 键 | 镜像模式 compose 由 `scripts/check-compose.cjs` 强制校验「不得含 `build:`」 |

### 3.5 环境差异类

| # | 现象 | 排查思路 | 解决 |
|---|---|---|---|
| 15 | `docker.sock` 挂载未生效，服务发现显示「不可用」（`ENOENT`） | 容器内 `ls /var/run/docker.sock` 不存在 → 定位到上传的是中间版 compose（socket 行仍被注释） | 重传最终版 compose 并 `up -d`；修复后 `available:true`、18/19 容器、首页新增「Docker 服务 15」分组 |
| 16 | `ghcr.io` 拉取不畅 | 国内加速器多只覆盖 Docker Hub | `.env` 里用 `NAVI_IMAGE` 换镜像站；兜底用 `docker save/load` 跨机搬运 |
| 17 | GitHub 推送间歇失败，疑似令牌无权限 | 主站 `github.com` 间歇不可达，而 `api.github.com` / `ssh.github.com` 始终通 | 先跑 `scripts/check-github-net.cjs` 定性；推送走 SSH 443 通道 |
| 18 | 架构兼容疑问 | 纯 JS + 零依赖 + 无原生模块 | x86_64 无需 `--platform`；硬门槛只在宿主（64 位、CPU 需 SSE4.2）；SELinux 发行版挂载加 `:Z` |

### 3.6 功能逻辑与质量类

| # | 现象 | 排查思路 | 解决 |
|---|---|---|---|
| 19 | 误报「备份文件被篡改」 | 局域网 HTTP 下 `crypto.subtle` 不存在 | 确立原则：**浏览器能力缺失 ≠ 失败**，降级返回「无法判断」交后端，后端才是权威 |
| 20 | Docker 未挂载时，所有发现卡片被一次性判为「已失效」 | `computeStale` 缺少「仅本次真正接入的来源参与判定」这一前提 | 加前置条件；未接入来源进 `skipped` 并如实告知（与 #19 同类错误） |
| 21 | 拖链接建卡时标题丢失 | `uri-list` 常只有裸 URL，标题在 `text/plain` 里，`||` 串联被短路 | 改为「谁带标题用谁」 |
| 22 | 全量回归偶发 1 项失败、单独跑必过 | 断言早于数据到达，取到了 loading 文案 | **凡「先加载后渲染」的元素，断言前必须等状态落定**（`waitForFunction`） |
| 23 | 弹窗字段增多后按钮点不到 | `.modal` 无高度上限，向视口两端溢出 | 封顶 `calc(100vh - 40px)` + 内部滚动 |
| 24 | 文档里的数字长期陈旧（「17 套」实为 19） | 这些数字不在任何断言范围内 | 文档里每个长期存在的数字都要进护栏；**改掉的旧值一律进 mustNots** |
| 25 | 基线文件自身不自洽（逐行和与合计行差 2） | 护栏只比对「文档 ↔ 基线」两边，两边一起错就都绿 | 补断言：合计行必须等于逐行之和、套件数等于条数 |
| 26 | 版本号四处硬编码，无单一来源 | 漏改任何一处都不报错，只表现为前后端显示不一致 | 补 3 条一致性护栏，并固化「发版前必改四处」清单 |

---

## 四、最终架构与关键配置

### 4.1 容器结构

```
┌─ 容器 navi（node:22-alpine，root 运行）────────────────┐
│  /app/server.js      入口：路由 / 鉴权闸门 / 静态托管    │
│  /app/discovery.js   服务发现（Docker API / 本机端口）   │
│  /app/status.js      状态板采集（全部可降级）           │
│  /app/zip.js         手写 ZIP 读写                      │
│  /app/public/**      前端（index/app.js/style.css/icons）│
│  /app/data  ◄──────  挂载点（唯一可写、唯一需备份）      │
│      ├─ config.json  站点配置（分组 + 卡片）             │
│      └─ uploads/     图床库与 Logo                      │
└──────────────────────────────────────────────────────────┘
     端口 80/tcp  ←→  宿主 18880（IPv4/IPv6 双栈）
     可选 /var/run/docker.sock:ro  ←→  宿主 socket（服务发现）
```

依赖方向：`server.js → discovery.js / status.js / zip.js`，单向无环；后端**只用 Node 内置模块**。

### 4.2 目录约定

| 路径 | 性质 | 说明 |
|---|---|---|
| `server.js` 等 4 个模块 | 程序，可重建 | 任一改动都需重建镜像 |
| `public/**` | 程序，可重建 | 含生成产物 `icons/`、`js/icon-map.js`、`js/pinyin.js`（**要入库**，改生成器后重跑） |
| `data/` | **唯一不可再生数据** | 路径挂载不随容器生命周期；只在手动删 / `down -v` / 迁移漏拷时丢 |
| `docs/`、`preview.html` | 文档 | `overview/roadmap/showcase/site` 四个刻意**不进发布包**（维护视角） |
| `test/`、`scripts/` | 质量与运维 | 19 套件 + 各类自检/判定脚本 |
| `share/` | 构建产物 | 由 `scripts/build-share.cjs` 生成，不手改 |

### 4.3 启动方式（三选一）

| 方式 | 适用 | 要点 |
|---|---|---|
| 源码构建 | 需要改代码 | `docker compose -f docker-compose.yml up -d --build` |
| **预构建镜像（推荐）** | 生产/NAS | `docker compose -f docker-compose.image.yml pull && ... up -d` |
| 裸 Node | 无 Docker 环境 | 直接 `node server.js`，数据落在项目内 `data/` |

统一前置条件：`data/config.json` 必须已存在；`NAVI_PASSWORD` 必须设置（compose 用 `${NAVI_PASSWORD:?}` 强制校验，缺失直接退出，不会静默变成无密码开放）。

### 4.4 运行依赖

- 宿主：64 位、CPU 支持 SSE4.2；SELinux 发行版挂载写 `./data:/app/data:Z`。
- 运行时：镜像内含 Node 22，**零外部 npm 依赖**，无需联网即可启动。
- 可选能力：`docker.sock` 只读挂载（服务发现）、图标在线探测（离线环境可用 `NAVI_ICON_PROBE=0` 关闭）。
- 网络：容器监听 80，对外端口由 compose 映射；IPv6 在宿主启用时自动双栈。

---

## 五、经验沉淀与后续优化建议

### 5.1 可复用的做法

1. **文档里的每个数字都要有断言守着**。无护栏的数字一定最先烂掉 —— 本次就抓出「17 套」（实为 19）、基线文件自身不自洽等长期问题。改动后的旧值同时写进 `mustNots`，才能防住「把旧包发上线」。
2. **能力缺失必须降级，不能判失败**。局域网 HTTP 下浏览器能力缺失是常态（本次踩到两次：`crypto.subtle`、Docker 未挂载），一律返回「无法判断」并交后端，绝不能误报成产品缺陷。
3. **HTML 交付物必须过自检**（标签栈 + 内联 JS 编译 + id 引用 + 外链扫描）。一个跨行字符串就能让整页交互全死，而页面看起来完全正常。
4. **发布类资产设唯一生产者**。截图被别的套件覆盖过一次，且「跑一次回归就覆一次」并随发布包上线 —— 必须用护栏把写者钉死。
5. **排障先拿事实再下结论**：`docker inspect` 读 compose 标签定位部署目录、GHCR 匿名令牌三连验证标签、`check-deploy.cjs` 远程判定线上新旧 —— 都避免了「看起来像权限问题其实是网络」这类误判。
6. **UI 断言要真的去点按钮**。弹窗溢出这种「1920 宽屏肉眼看不出」的缺陷，只有真实点击才抓得到。

### 5.2 遗留风险点

| 风险 | 说明 | 建议 |
|---|---|---|
| 版本号仍无单一来源 | 四处硬编码，虽有护栏但本质上靠纪律 | 后续可抽出单一来源（如构建期注入），从机制上消除 |
| `latest` 漂移 | 默认 `pull_policy` 下 Docker 复用本地镜像，`latest` 可能悄悄变旧 | 坚持用显式 `docker compose pull` 升级，或钉死 `NAVI_TAG=X.Y.Z` |
| 升级动作易漏 | **推 main 只出 `edge`，只有打 `v*.*.*` 才更新 `latest`**；漏打标签 = NAS 上永远拉不到新功能 | 已写进发版清单，发版时逐项勾 |
| socket 的安全代价 | 挂载后容器拥有等价于宿主 root 的 Docker 控制权 | 仅在可信内网开启；保持 `:ro` 并定期审视 |
| P2 剩余 5 项 | 拆前端单文件（**刻意搁置**）、自定义壁纸、通用反代规则导入、Release Notes 自动化、版本节奏 | 见 5.3 |
| 图床库与标签尚未使用 | 线上为 0 条新功能数据 | 非故障，属未启用 |

### 5.3 迭代方向

1. **拆前端单文件**（已评估、刻意搁置）：纯重构无行为收益，且 `app.js` 是多个 UI 套件共同依赖的单一文件，风险收益不对称；若要动，应独立做一轮并在全绿状态下进行。
2. **自定义壁纸与主题增强**：当前仅日/夜双主题，可引入壁纸与强调色配置。
3. **通用反代规则导入**：从 Nginx Proxy Manager / Traefik 等规则批量生成卡片。
4. **Release Notes 自动化**：流水线接入自动发版说明，减少手工维护。
5. **版本号单一来源**：配合上一条，从机制上消除四处硬编码。
6. 运维侧：可考虑为状态板增加历史水位与告警阈值，让「家庭服务健康度」更有洞察力。

