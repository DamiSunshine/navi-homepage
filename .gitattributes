# 默认统一为 LF，避免 Windows 上提交时把 CRLF 带进仓库造成整文件 diff
* text=auto eol=lf

# Windows 批处理必须使用 CRLF，否则 cmd.exe 可能解析异常
*.cmd text eol=crlf
*.bat text eol=crlf

# Shell 脚本固定 LF
*.sh text eol=lf

# 二进制文件不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.pdf binary
*.ico binary
